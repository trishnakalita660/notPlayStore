<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.playstore">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".DrawerActivities.AppsActivity"></activity>
        <activity android:name=".DrawerActivities.AccountsActivity"></activity>
        <activity android:name=".DrawerActivities.NotificationActivity"></activity>
        <activity android:name=".DrawerActivities.PlayAndProtectActivity"></activity>
        <activity android:name=".DrawerActivities.PaymentActivity"></activity>
        <activity android:name=".DrawerActivities.SettingsActivity"></activity>
        <activity android:name=".DrawerActivities.SubscriptionActivity"></activity>
        <activity android:name=".DrawerActivities.WishlistActivity"></activity>






        <activity
            android:name=".MainActivity"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>